import EventDispatcher from '../EventDispatcher';

/**
 * モデルのベースクラスです。
 */
export default class BaseModel extends EventDispatcher {

  /**
   * コンストラクター
   * @constructor
   */
  constructor() {
    super();
  }

}
