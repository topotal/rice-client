import { StyleSheet } from 'react-native';

/**
 * タイムライン画面のスタイルです。
 */
export default StyleSheet.create({
  wrapper: {
    flex: 1,
    padding: 10
  },
  recipeButton: {
    marginBottom: 10
  },
  cookButton: {
    backgroundColor: '#FF9B00'
  }
});
